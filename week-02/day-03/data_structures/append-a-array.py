nimals = ["kuty", "macsk", "cic", "ar"]
a = "a"

for i in range(len(nimals)):
    print(nimals[i] + a)

# - Create a variable named `nimals`
#   with the following content: `["kuty", "macs", "cic"]`
# - Add all elements an `"a"` at the end
